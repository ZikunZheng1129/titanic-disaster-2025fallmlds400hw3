# Optional R packages for a richer workflow.
# If you want to install them, uncomment the block below and rebuild.
#
# packages <- c("tidyverse", "glmnet")
# for (pkg in packages) {
#   if (!requireNamespace(pkg, quietly = TRUE)) {
#     install.packages(pkg, repos = "https://cloud.r-project.org")
#   }
# }

cat("install_packages.R: nothing to install (base R only).\n")